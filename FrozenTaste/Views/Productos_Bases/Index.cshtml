@model IEnumerable<FrozenTaste.Models.Productos_Bases>

@{
    ViewBag.Title = "Productos por base";
}

<h2>Productos por base</h2>

<p>
    @Html.ActionLink("Nuevo", "Create", new { }, new { @class = "btn btn-primary" })
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Bases.Base)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Productos.Producto)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Bases.Base)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Productos.Producto)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id = item.Id_Productos_Base }, new { @class = "btn btn-primary" }) 
            @Html.ActionLink("Details", "Details", new { id = item.Id_Productos_Base }, new { @class = "btn btn-primary" }) 
            @Html.ActionLink("Borrar", "Delete", new { id = item.Id_Productos_Base }, new { @class = "btn btn-danger" }) 
        </td>
    </tr>
}

</table>
